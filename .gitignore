# dependencies get reinstalled from package.json, never committed
node_modules/

# secrets (database passwords etc.) stay local - .env.example shows the shape
.env

# build output
dist/
build/

# editor + OS junk
.DS_Store
*.log
